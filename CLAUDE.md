# 프로듀서그룹 도트 웹사이트 — 작업 지침

이 파일은 Claude Code가 매 세션 시작할 때 읽는 프로젝트 규칙입니다.
저장소 루트에 두세요. 결정된 사항은 여기 적힌 대로만 따르고, 새로 정한 것은 여기에 추가합니다.

시안 파일 `dot-prototype.html` 이 시각적 기준입니다. 구조·색·간격은 이 파일을 따르되,
코드는 그대로 옮기지 말고 아래 구조로 새로 짭니다. (시안은 단일 파일이라 유지보수가 안 됩니다.)

---

## 1. 이 사이트가 무엇인가

프로듀서그룹 도트 — 2014년 결성된 공연예술 프로듀서 콜렉티브. 네 명의 여성 프로듀서.
핵심 가치는 **창의성 · 독립성 · 수평성**이고, 이 세 가지가 디자인 결정의 기준입니다.

가장 중요한 구조적 원칙:

> **네 프로듀서의 색이 귀속(attribution) 표시로 작동한다.**
> 모든 작업에 그 일을 맡은 프로듀서의 색점이 찍히고, 색점을 누르면 그 사람의 작업만 남는다.
> 색은 장식이 아니라 데이터다.

이 원칙을 깨는 변경은 하지 않습니다. 예를 들어 메뉴에 프로듀서 색을 쓰면 안 됩니다.

---

## 2. 사이트맵과 라우팅

```
/                     홈 — 모자이크 + Now
/works                작업 인덱스 (공연 / 프로젝트 토글 + 프로듀서 색점 필터)
/works/:id            작업 상세
/artists              예술가
/artists/:id          예술가 상세
/about                도트 소개 · 프로듀서 4인 · 둥지230
/producers/:id        프로듀서 상세 (각자의 색이 페이지 배경)
```

메뉴는 **Works · Artists · About** 셋. Now는 홈 하단에 있고 메뉴가 아닙니다.
둥지230은 About 안에 있고 최상위 메뉴가 아닙니다.

라우팅은 해시(`#/works/sync-de-sync`) 또는 정적 HTML 파일 중 하나로.
GitHub Pages에 올리므로 서버 라우팅은 쓸 수 없습니다.

---

## 3. 색

```css
--cream:     #EAE4DA;   /* 바탕 */
--cream-hi:  #F7F3EC;   /* 빨강 면 위 본문 */
--panel:     #F3EEE5;   /* 상세 페이지 패널 */
--ink:       #1D1D1B;   /* 글자 */
--lavender:  #808BC5;   /* 구조색 — 사람 아님 */

/* 프로듀서 색 — 면(field)용 */
--blue:    #2A5982;   /* 이희진 */
--green:   #245E55;   /* 최봉민 */
--mustard: #EAC119;   /* 박지선 */
--red:     #C63F3E;   /* 송미선 */

/* 같은 색의 점·글자용 진한 짝 */
--blue-d:    #004575;
--green-d:   #014E45;
--mustard-d: #776101;
--red-d:     #9E0117;
```

**색 사용 규칙**

- 한 화면에 큰 색면은 하나. 예외: 홈 모자이크, About의 프로듀서 4칸.
- **머스타드 면 위에는 잉크 글씨. 나머지 세 면 위에는 크림 글씨.** 빨강 면의 본문만 `--cream-hi`.
- 크림 바탕 위의 색점·색글씨는 반드시 진한 짝(`-d`)을 쓴다. 머스타드는 크림 위에서 대비가 1.4라 보이지 않는다.
- 라벤더는 구조색이다. 사람에게 배정하지 않는다. 담당 미지정 표시에도 쓴다.
- 로고는 검정 + 액센트 점 하나. 액센트 점은 페이지마다 네 색 중 무작위(그 페이지의 면 색은 제외).

**색–사람 매핑은 아직 미확정입니다.** 위 배정은 명함 뒷면 판독 기준입니다.
확정 전까지 `data/producers.json` 한 곳에서만 색을 정의하고, 코드 어디에도 하드코딩하지 마세요.

---

## 4. 서체

- 제목·메뉴 — Archivo 600 (라틴) / Pretendard (한글)
- 본문 — Pretendard 300
- 메타(연도·색인코드·크레딧 역할·태그) — IBM Plex Mono 400

`word-break: keep-all` 을 body에 겁니다. 한글이 단어 중간에서 끊기면 안 됩니다.

레트로로 넘어가지 않게 주의하세요. 웜 크림 + 흙빛 색은 서체가 조금만 복고풍이면
1970년대 포스터가 됩니다. 조밀하고 중립적인 현대 산세리프를 유지합니다.

---

## 5. 화면별 규칙

### 홈

4열 × 4행 모자이크, `gap: 0`, `border-radius: 0`, 높이 96vh.
행 높이는 `1fr 1fr 1fr 1.45fr` — 마지막 행이 사진 띠입니다.

```
r1  [ 로고 (2열) ][ 세로사진 (r1-3) ][ Works    ]
r2  [ P1 ][ P2 ]                    [ Artists  ]
r3  [ P3 ][ P4 ]                    [ About    ]
r4  [ 파노라마 사진 (2열) ][ 파노라마 사진 (2열) ]
```

- **프로듀서 네 칸의 자리는 매 방문마다 무작위로 섞는다.** 메뉴 칸은 자리를 지킨다.
- 모자이크 아래는 **Now**. 회차 단위로 한 줄씩 나오고 `지난 / 진행 중 / 예정` 표시가 붙는다.
  **무엇이 나오고 어떤 차례로 나오는지는 `data/now.json` 의 배열이 정한다.**
  배열에 없는 작업은 Now에 나오지 않는다(Works에는 그대로 남는다).
  배열에 있는 작업은 앞뒤 3개월 안의 회차가 한 줄씩, 회차가 없으면 `status: ongoing` 일 때 한 줄.
  단 `지난` 것은 배열 차례와 상관없이 늘 맨 아래로 내려간다.
  날짜순으로만 두면 원하는 차례가 안 나오고, 진행 중인 일 중 무엇을 앞세울지는 사람이 정할 일이다.
- Now 아래 사진 3장.
- 푸터에 인스타그램·유튜브 아이콘.

모바일은 2열 9행으로 접힙니다.

### Works

매스트헤드는 제목만. 빵부스러기 링크는 넣지 않습니다(상단 메뉴와 중복).

3열 메이슨리 카드 그리드(`columns: 3`). 카드 = 이미지 → 제목(밑줄) → 예술가 → 색점·연도.
상단에 `전체 / 공연 / 프로젝트` 토글과 프로듀서 색점 필터. **둘은 동시에 걸립니다.**

표지 이미지가 없으면 담당 프로듀서 색의 색면 블록으로 대체(미지정은 라벤더).
임시 이미지에는 좌상단에 `임시 이미지` 라벨.

### 작업 상세

```
제목 블록 (색점 · 종류 · 연도 / 큰 제목 / 부제)
────────────────────────────────────────
[ 패널 78% : 리드 + 본문 + '+' 아코디언 ]   [ 사진이 패널 밖으로 걸쳐 나옴 ]
사진 4장 가로 띠
Explore more — 카드 4장
```

아코디언은 `일정 · 정보 · 크레딧 · 참가자 · 링크` 순. 일정만 열린 상태로 시작.
패널 오른쪽 사진은 절대 위치라 높이가 어긋납니다. **이미지 로드 후 패널 높이를 JS로 맞춰야 합니다.**

### Artists

자유 배치 캔버스. **높이 96vh**, 항목마다 지름을 가집니다.
좌표는 새로고침할 때마다 무작위로 뽑습니다(`artists.json` 의 좌표는 실패했을 때의 되돌림용).

겹침 검사는 원이 아니라 **'원 + 라벨'을 감싸는 사각형**으로 합니다 —
폭 `max(지름, 236)`, 높이 `지름 + 92`, 사각형끼리 최소 34px(간격 28 + 플로팅 6).
원만 검사하면 라벨이 옆 원을 덮습니다. 캔버스가 72vh 였을 때는 이 사각형이 들어갈
자리가 없어 1440px 폭에서 열 번 중 아홉 번 고정 배치로 되돌아갔습니다.

```
(2%, 0%, 26vh) (29%, 40%, 17vh) (70%, 3%, 23vh) (9%, 58%, 18vh) (65%, 54%, 15vh)
```

원 안은 흑백, 호버 시 컬러 + 살짝 확대. 라벨은 원 아래.
좌표는 `data/artists.json` 에 넣어 데이터로 관리합니다. 여섯 명 이상이면 좌표를 늘려야 합니다.
모바일은 100px 원 + 세로 목록으로 되돌립니다.

### 프로듀서 상세

**페이지 전체가 그 사람의 색.** 상단 메뉴바도 같이 물듭니다(`background: inherit`).
왼쪽 본문, 오른쪽에 `도트에서 맡은 작업`(색점으로 자동 필터) · `연결` · `다른 프로듀서` 세 명.
박지선 페이지에는 리소그래프풍 일러스트가 크림 액자에 담겨 들어갑니다.

### About

```
About 제목 → 3단 소개문 → Producers (색면 4칸) → 둥지230 (잉크 배너) → 연결
```

둥지230은 페이지에서 유일한 어두운 면입니다. 라벤더 큰 원이 오른쪽 위에 걸치고,
제목은 60px 가까이, 링크는 테두리 버튼. 작게 만들면 안 됩니다 — 이미 한 번 안 보인다고 지적받았습니다.

---

## 6. 데이터

구글 시트가 원본입니다. 시트 → CSV → `data/*.json` 으로 빌드하고, 사이트는 JSON만 읽습니다.
시트 구조는 `도트-자료취합.xlsx` 를 그대로 따릅니다.

```
data/
  producers.json   id, name{ko,en}, color, role{ko,en}, bio{ko,en}, email, links, photo
  artists.json     id, name{ko,en}, field{ko,en}, producers[], bio{ko,en}, links, photo, pos{x,y,d}
  works.json       id, type, title{ko,en}, subtitle{ko,en}, artist{ko,en}, producers[],
                   year, status, lead{ko,en}, body{ko,en}, venue{ko,en}, produced{ko,en},
                   commission{ko,en}, keywords{ko,en}, cover, homeFeature
  credits.json     workId, order, role{ko,en}, name{ko,en}
  (runs.json 은 2026-08-15 works.json 안으로 옮겼습니다 — 아래 참고)
  links.json       workId, label{ko,en}, url
  photos.json      file, subject, photographer{ko,en}, year, consent, isCover, caption{ko,en}
  now.json         홈 Now에 나올 작업 ID 배열. 적힌 차례대로 그린다.
```

**회차(runs)는 작품 안에 있습니다.** `works.json` 의 각 항목이 `runs` 배열을 가집니다.

```
runs[]  start, end, city{ko,en}, venue{ko,en}, kind{ko,en}, dotRole{ko,en}, link
        (+ time, date{ko,en}, note{ko,en})
kind    초연 · 재연 · 투어 · 공동제작
dotRole 도트가 그 회차에서 맡은 역할. production 이 아니면 화면에 따로 적는다.
```

작품과 회차를 두 파일에 나눠 두면 한쪽만 고쳐져 반드시 어긋납니다.
Works 카드의 연도 범위·도시, 정렬 기준(가장 최근 회차), 홈 Now 의 날짜·장소가
모두 이 배열에서 나옵니다.

**국문/영문은 처음부터 `{ko, en}` 객체로.** 나중에 붙이면 전부 다시 손대야 합니다.
`en` 이 비면 `ko` 를 그대로 내보내는 헬퍼 하나(`t(field, lang)`)를 만들고 전부 그걸 거칩니다.

작업 ID는 영문 소문자와 하이픈만. 이미지 파일명도 같은 ID를 씁니다(`sync-de-sync-01.jpg`).

---

## 7. 이미지

```
img/
  works/<work-id>/01.jpg …
  artists/<artist-id>.jpg
  producers/<producer-id>.jpg
  home/<name>.jpg          홈 전용 파노라마
```

- 작업 사진: 가로 3:2 최소 1600px, 세로 3:4 최소 1200px
- **홈 아래 파노라마: 5:2 로 미리 잘라둔 파일을 쓴다.** CSS 자동 크롭에 맡기면 인물의 머리가 잘립니다.
- 포스터는 원본 비율, `object-fit: contain` + 크림 배경
- 모든 사진에 촬영자 크레딧이 있어야 합니다. 없으면 올리지 않습니다.
- 구글 포토·드라이브 원격 URL을 그대로 `<img src>` 에 넣지 마세요. 앨범 설정이 바뀌면 전부 깨집니다.

---

## 8. 하지 말 것 — 실제로 겪은 함정

1. **데이터 구조만 만들고 렌더링 코드를 안 만드는 일.**
   개인 사이트에서 반복해서 겪었습니다. 새 필드를 추가하면 그걸 화면에 그리는 코드를
   같은 커밋에서 함께 만드세요. 안 그러면 데이터는 있는데 화면에 아무것도 안 나옵니다.

2. **"이제 안 쓴다"를 파일 삭제로 옮기는 일.**
   화면에서 빼는 것과 파일을 지우는 것은 다릅니다. 사진이 사라진 적이 있습니다.
   뺄 때는 화면에서만 빼고 파일은 남깁니다.

3. **인라인 `style` 로 그리드 위치를 지정하는 일.**
   인라인 스타일은 미디어 쿼리를 이깁니다. 데스크톱은 멀쩡한데 모바일이 통째로 깨집니다.
   그리드 위치는 반드시 CSS 클래스로.

4. **`--line` 같은 토큰을 하드코딩하는 일.**
   프로듀서 페이지는 배경색이 바뀌므로 선 색도 같이 뒤집혀야 합니다.
   `rgba(29,29,27,.45)` 를 CSS에 직접 쓰면 어두운 배경에서 선이 사라집니다.

5. **⬡ 같은 특수문자를 폰트에 맡기는 일.**
   `⬡⬡의 섬` 은 환경에 따라 `○○` 로 보입니다. SVG로 그리세요.

---

## 9. 배포

GitHub Pages, `main` 브랜치 루트. 개인 사이트와 같은 방식입니다.
도메인은 어느 정도 마무리된 뒤에 구입해 연결합니다.

빌드가 필요하면 GitHub Actions에서 시트 CSV를 받아 JSON을 만들고 커밋하는 워크플로 하나만 둡니다.
로컬 빌드 도구를 요구하지 마세요 — 네 명이 각자 컴퓨터에서 만질 수 있어야 합니다.

---

## 10. 아직 안 정해진 것

- 색–사람 매핑 (명함 뒷면 기준 vs 브리프 기준이 어긋남)
- 로마자 성-이름 순서 (Park Jisun / Jisun Park)
- Artists 원 안에 인물 사진을 넣을지 작업 이미지를 넣을지
- 〈관통 18〉·〈움직이는 숲〉 등 6건의 담당 프로듀서
- 앰비규어스 〈더 벨트〉를 Works에 넣을지
- 프로듀서 3인의 본인 소개문
- ~~둥지230을 도트 프로젝트로 셀지~~ → **작업이 아니다.** 2026-08-14 `works.json` 에서 뺐습니다.
  About의 어두운 배너와 장소(venue) 표기로만 남습니다.

**정정 (2026-08-14)** — 〈무용 × 기술 창작 랩〉은 도트 프로젝트가 아니라 박지선 개인 프로젝트입니다.
`data/works.json` 에서 뺐습니다. 도트 프로젝트는 〈⬡⬡의 섬 : 강화도〉·〈TNN The Next Normal〉·
〈기후변화 예술가 레지던시〉·〈Aesth:ethics〉·〈아시아 프로듀서 플랫폼 APP〉 다섯 건입니다(둥지230 미정).
시안(`_ref/dot-prototype.html`)에는 아직 들어 있으니 시안에서 데이터를 옮길 때 다시 딸려오지 않게 하세요.

이 중 하나라도 코드에 하드코딩하지 마세요. 전부 데이터로 두면 나중에 한 줄만 고치면 됩니다.
